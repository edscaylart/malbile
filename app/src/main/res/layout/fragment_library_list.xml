<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:fab="http://schemas.android.com/apk/res-auto"
             android:layout_width="match_parent"
             android:layout_height="match_parent">

    <include layout="@layout/empty"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />

    <com.melnykov.fab.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_alignParentRight="true"
        android:layout_marginRight="@dimen/fab_button_bottom"
        android:layout_marginBottom="@dimen/fab_button_bottom"
        android:src="@drawable/ic_edit"
        fab:fab_colorNormal="@color/primaryBlue500"
        fab:fab_colorPressed="@color/accentPinkA200"
        android:visibility="gone"
        android:id="@+id/listEdit"/>
</FrameLayout>